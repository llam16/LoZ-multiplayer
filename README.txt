Sound class only works with Java 10 or older as it requires javafx

Two player game requiring same network and change hostname to IP address in ClientScreen.java or stay localhost if playing on the same computer